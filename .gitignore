# Terraform state files (contain sensitive infrastructure data)
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup

# Terraform variables (may contain secrets like passwords, keys)
terraform.tfvars
*.auto.tfvars

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
*.tfplan

# AWS credentials
.aws/
credentials

# Environment variable files
.env
*.env
